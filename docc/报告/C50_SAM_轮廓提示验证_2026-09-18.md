# SAM2 轮廓提示验证

本次使用用户第二版红色杯体轮廓，从每行红色边界跨度插值得到约 2,177 像素初始掩码。它是近似提示，不是精确像素真值；SAM 仍输入未涂色的原始视频图像。

源码核查：默认 `use_mask_input_as_output_without_sam=True` 会直接保留输入掩码作为种子帧输出，随后编码记忆追踪，不能将这一行为称为自动修正种子帧。实验同时将该开关设为 False 测试 SAM 实际解码轮廓提示。只改变实验预测器，不改训练或真机控制代码。

ep20 533–543，共 11 帧，以 538 为种子，两方向传播，对照如下：

| 方法 | 种子输出面积 | 与近似轮廓的一致性，非准确率 | 空帧 / >50% 面积突跳 |
| --- | --- | --- | --- |
| 第二版点提示 | 2,508 | 0.5622 | 0 / 0 |
| 轮廓提示 + SAM 解码 | 2,819 | 0.7389 | 0 / 0 |
| 保留轮廓种子 + SAM 追踪 | 2,172 | 0.9913 | 0 / 0 |

已视觉查看 538 和 543 对照：相比零散点提示，轮廓方案的杯体前端背景溢出明显收敛。不能把面积变小当作准确，不能把 0.9913 当成预测精度：该方案就是保留输入种子。

四联图顺序：近似轮廓、第二版点提示、SAM 解码轮廓、保留种子后追踪。非种子帧第一列仅为原图，不复制种子轮廓。

最终完整结果：`outputs/mask_inject_phase3_20260918/sam_contour_ep020_533_543_checked/`；工具 `scripts/check_c50_sam_contour.py`。首次运行在 JSON 保存遇到 NumPy int64 序列化错误，修复后 2 项轮廓回归通过，在新目录完整复测。首次部分产物仍保留于 `sam_contour_ep020_533_543/`，不要作为完整运行记录使用。

下一步：先核查四联图的杯体边界，合格后将同一提示方式扩展到完整 ep20，检查遮挡前后、抓起及放下全过程，再扩展到其他训练 episode。当前只是一个种子及局部窗口验证；不将它直接当作已完成 40 episode 正式标签，不启动训练，不替换已有 mask。
