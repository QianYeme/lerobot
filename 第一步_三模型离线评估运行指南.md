# 第一步：三模型 100k checkpoint 离线动作评估运行指南

日期：2026-09-15
项目：`/root/autodl-tmp/lerobot/lerobot-main`
适用模型：`C50_ACT_s1000`、`C50_DET_s1000`、`C50_INJECT_s1000`

## 1. 本步目标

在固定的 10 个留出 episode 上，使用同一套参数串行评估三个 100k checkpoint：

```text
7,11,13,14,16,27,35,38,40,43
```

本步会计算：

- 三个模型的零 z 推理动作误差 `inference_l1_loss`；
- DET 和 INJECT 的 `det_cls_loss`、`det_reg_loss`、`det_ctr_loss`；
- ACT、DET、INJECT 的统一汇总表。

本步不连接机器人，不发送机械臂动作，也不修改 checkpoint。它会在 `outputs/formal1_C50_phase1/offline_eval_100k/` 新建评估结果。

> 当前评估脚本输出 JSON 时只把 episode 清单记录成首尾值 `[7, 43]`，不能表达中间的非连续编号。因此本流程另外保存 `validation_episodes.txt`，该文件才是本次完整验证集清单。

## 2. 已核对的前置条件

- Python：`/root/miniconda3/bin/python`（Python 3.12.3）
- GPU：NVIDIA GeForce RTX 5090，检查时显存空闲
- 数据集：`数据集/formal1_C/meta/info.json` 存在
- 三个 `checkpoints/100000/pretrained_model` 均包含配置和权重
- 当前磁盘可用空间约 57 GB

## 3. 启动 screen

先登录服务器并进入项目：

```bash
ssh -p 23530 root@connect.westd.seetacloud.com
cd /root/autodl-tmp/lerobot/lerobot-main
screen -S C50_offline_eval
```

进入新的 screen 后，将下一节的完整代码一次性粘贴执行。开始运行后，可按 `Ctrl+A`，再按 `D` 脱离；SSH 断开不会终止评估。

## 4. 完整运行代码

```bash
set -euo pipefail

export PROJECT=/root/autodl-tmp/lerobot/lerobot-main
export DATA_ROOT="$PROJECT/数据集/formal1_C"
export PYTHONNOUSERSITE=1
export PYTHON=/root/miniconda3/bin/python
export VAL_EPISODES='7,11,13,14,16,27,35,38,40,43'
export OUT_DIR="$PROJECT/outputs/formal1_C50_phase1/offline_eval_100k"

cd "$PROJECT"

# 保护已有结果：目标目录存在时停止，不覆盖。
if [[ -e "$OUT_DIR" ]]; then
  echo "ERROR: output directory already exists: $OUT_DIR" >&2
  echo "Move it aside or change OUT_DIR before rerunning." >&2
  exit 1
fi
mkdir -p "$OUT_DIR"

# 保存本次评估的可复现信息。
printf '%s\n' "$VAL_EPISODES" > "$OUT_DIR/validation_episodes.txt"
git rev-parse HEAD > "$OUT_DIR/code_commit.txt"
git status --short > "$OUT_DIR/code_status.txt"

{
  echo "project=$PROJECT"
  echo "dataset_root=$DATA_ROOT"
  echo "validation_episodes=$VAL_EPISODES"
  echo "started_at=$(date --iso-8601=seconds)"
  "$PYTHON" --version
  nvidia-smi --query-gpu=name,memory.total,memory.used --format=csv,noheader
} | tee "$OUT_DIR/preflight.log"

# 数据与 checkpoint 完整性检查。任一检查失败都会立即停止。
test -f "$DATA_ROOT/meta/info.json"

for RUN in C50_ACT_s1000 C50_DET_s1000 C50_INJECT_s1000; do
  CKPT="$PROJECT/outputs/train/$RUN/checkpoints/100000/pretrained_model"
  test -f "$CKPT/config.json"
  test -s "$CKPT/model.safetensors"
  test -f "$CKPT/policy_preprocessor.json"
  test -f "$CKPT/policy_postprocessor.json"
done

run_eval() {
  local RUN="$1"
  local CKPT="$PROJECT/outputs/train/$RUN/checkpoints/100000/pretrained_model"
  local RESULT="$OUT_DIR/${RUN}_offline.json"
  local LOG="$OUT_DIR/${RUN}_offline.log"

  echo "===== START $RUN $(date --iso-8601=seconds) =====" | tee "$LOG"
  "$PYTHON" -m lerobot.scripts.offline_eval_act_det \
    --checkpoint "$CKPT" \
    --dataset.repo_id QYyyyyyyy/formal1_C \
    --dataset.root "$DATA_ROOT" \
    --episodes "$VAL_EPISODES" \
    --dataset.video_backend pyav \
    --annotation-dir "$DATA_ROOT/annotations" \
    --batch-size 8 \
    --num-workers 0 \
    --output "$RESULT" \
    2>&1 | tee -a "$LOG"

  test -s "$RESULT"
  echo "===== END $RUN $(date --iso-8601=seconds) =====" | tee -a "$LOG"
}

# 单 GPU 串行执行，避免争抢显存并保证条件一致。
run_eval C50_ACT_s1000
run_eval C50_DET_s1000
run_eval C50_INJECT_s1000

# 汇总三个 JSON；只使用 Python 标准库，不依赖 pandas。
"$PYTHON" - <<'PY'
import csv
import json
import os
from pathlib import Path

out_dir = Path(os.environ["OUT_DIR"])
runs = ["C50_ACT_s1000", "C50_DET_s1000", "C50_INJECT_s1000"]
metric_names = [
    "inference_l1_loss",
    "det_cls_loss",
    "det_reg_loss",
    "det_ctr_loss",
    "mask_loss",
]

rows = []
for run in runs:
    path = out_dir / f"{run}_offline.json"
    with path.open(encoding="utf-8") as f:
        result = json.load(f)
    metrics = result["metrics"]
    rows.append({"run": run, **{name: metrics.get(name, "") for name in metric_names}})

summary_path = out_dir / "summary.csv"
with summary_path.open("w", newline="", encoding="utf-8") as f:
    writer = csv.DictWriter(f, fieldnames=["run", *metric_names])
    writer.writeheader()
    writer.writerows(rows)

print("\n===== Summary =====")
for row in rows:
    values = []
    for name in metric_names:
        value = row[name]
        if value != "":
            values.append(f"{name}={float(value):.6f}")
    print(f"{row['run']}: " + ", ".join(values))
print(f"\nSummary saved to {summary_path}")
PY

date --iso-8601=seconds > "$OUT_DIR/COMPLETED.txt"

echo
echo "ALL THREE OFFLINE EVALUATIONS COMPLETED"
echo "Results: $OUT_DIR"
column -s, -t "$OUT_DIR/summary.csv" 2>/dev/null || cat "$OUT_DIR/summary.csv"
```

## 5. 查看进度

从 screen 脱离后，可在另一个 SSH 会话中查看：

```bash
cd /root/autodl-tmp/lerobot/lerobot-main
screen -ls
screen -r C50_offline_eval
```

也可以不进入 screen，直接跟踪当前日志：

```bash
tail -f outputs/formal1_C50_phase1/offline_eval_100k/C50_ACT_s1000_offline.log
tail -f outputs/formal1_C50_phase1/offline_eval_100k/C50_DET_s1000_offline.log
tail -f outputs/formal1_C50_phase1/offline_eval_100k/C50_INJECT_s1000_offline.log
```

`tail -f` 用 `Ctrl+C` 退出，只会停止查看日志，不会停止 screen 中的评估。

## 6. 完成验收

运行结束后执行：

```bash
cd /root/autodl-tmp/lerobot/lerobot-main
OUT_DIR=outputs/formal1_C50_phase1/offline_eval_100k

test -s "$OUT_DIR/C50_ACT_s1000_offline.json"
test -s "$OUT_DIR/C50_DET_s1000_offline.json"
test -s "$OUT_DIR/C50_INJECT_s1000_offline.json"
test -s "$OUT_DIR/summary.csv"
test -s "$OUT_DIR/COMPLETED.txt"

cat "$OUT_DIR/validation_episodes.txt"
column -s, -t "$OUT_DIR/summary.csv" 2>/dev/null || cat "$OUT_DIR/summary.csv"
```

五个 `test -s` 全部退出 0，且屏幕显示汇总表，才算第一步完成。预期目录内容：

```text
offline_eval_100k/
├── COMPLETED.txt
├── code_commit.txt
├── code_status.txt
├── preflight.log
├── validation_episodes.txt
├── C50_ACT_s1000_offline.json
├── C50_ACT_s1000_offline.log
├── C50_DET_s1000_offline.json
├── C50_DET_s1000_offline.log
├── C50_INJECT_s1000_offline.json
├── C50_INJECT_s1000_offline.log
└── summary.csv
```

## 7. 如何解读第一步结果

1. 首先比较三个模型的 `inference_l1_loss`，越低表示零 z 推理动作在留出集上越接近演示动作。
2. `det_cls_loss`、`det_reg_loss`、`det_ctr_loss` 只在 DET/INJECT 之间比较，不能与 ACT 的动作 loss 直接比较。
3. 检测 loss 较低只能证明留出集上的检测辅助目标拟合较好，不能证明预测框 IoU、召回率或定位稳定性合格。
4. 这一结果不能单独证明真机成功，也不能证明模型真正利用了杯子视觉；完成本步后，第二步应运行视觉敏感性评估。

本阶段只根据统一离线结果筛选候选模型，不继续增加训练步数，也不直接在服务器上执行机器人控制。

## 8. 常见失败处理

- `CUDA out of memory`：确认没有其他 GPU 进程；若仍不足，把三个模型的 `--batch-size` 都改为 `4`，使用新的 `OUT_DIR` 重跑并记录变更。
- 找不到 `python`：必须使用文档中的 `/root/miniconda3/bin/python`，不要依赖新 screen 的 PATH。
- 视频解码失败：确认参数仍为 `--dataset.video_backend pyav`，不要临时换成 torchcodec。
- 找不到标注：确认 `$DATA_ROOT/annotations` 与 checkpoint 配置指向同一份 `formal1_C` 数据。
- 中途失败：保留失败目录和日志，先判断原因；不要直接覆盖。修复后给 `OUT_DIR` 换一个新名称再运行。
