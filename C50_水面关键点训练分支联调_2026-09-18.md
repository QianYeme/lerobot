# 水面参考关键点：可选训练分支联调

## 范围

水面参考点辅助任务已接入ACTDetPolicy联合损失。默认关闭，不改变旧DET、INJECT、MASK参数键。旧MASK仍然是分割任务；新分支是关键点热图，不称为杯体分割。

本次仅工程联调，随机模型、真实图像与已有8状态预处理器，没有optimizer更新，没有正式训练或定位精度结论。

## 开关

后续训练配置需要显式设置以下参数，不能直接把旧MASK checkpoint当作训练好的关键点模型：

```text
--policy.use_water_keypoint=true
--policy.use_mask_guidance=false
--policy.mask_feature_inject=false
--policy.water_keypoint_labels=/root/autodl-tmp/lerobot/lerobot-main/outputs/water_center_pilot_20260918/keypoint_labels.json
--policy.water_keypoint_weight=0.1
--policy.aug_enable=false
```

以上是参数片段，不是完整训练启动命令。本步没有改动三组100k训练启动脚本。权重0.1仅用于联调，不是已验证最优值。

分支依赖已启用的top相机DET/FPN，采用独立辅助解码器、60×80 Gaussian热图（sigma=2网格像素），可见帧均值MSE。总损失增加`water_keypoint_weight * water_keypoint_loss`；日志包含可见数量和覆盖率。遮挡/未标注只忽略辅助损失，动作监督保持。

标签延迟到辅助训练前向时读取。正常动作推理不访问标签、不执行关键点解码器，也不需要episode/frame索引。训练缺少标签文件或索引报错。标签必须与实际输入图像尺寸匹配；尚未支持几何增强后的坐标变换。

随机遮挡增强可能遮住原本可见的参考点；当前未实现增强后的可见性重判，因此关键点模式要求关闭随机遮挡，联调直接关闭全部增强。不能在后续命令中无意沿用旧随机遮挡配置。

## 复现验证

```bash
cd /root/autodl-tmp/lerobot/lerobot-main
PYTHONPATH=src /root/miniconda3/bin/python tests/policies/act_det/test_water_keypoint.py -v
PYTHONPATH=src /root/miniconda3/bin/python scripts/check_c50_water_keypoint_integration.py
```

真实数据工具拒绝覆盖已有`integration_results.json`，避免抹掉运行证据；已有结果可直接阅读。首次结果另存为`integration_results_initial.json`，最终结果包含旧checkpoint参数键兼容检查。

联调使用episode20帧0、205、538、717，top/gripper两相机640×480、8状态、6动作。验证联合损失加权、辅助梯度进入decoder/FPN/backbone、205输出辅助梯度0、仅205帧时动作头梯度仍非零、辅助参数进入优化器、无标签/索引/辅助解码器的推理精确一致。默认模型参数键与旧20步DET checkpoint完全一致。配置保存/加载经PreTrainedConfig注册入口验证。

证据位于`outputs/water_center_pilot_20260918/integration_results.json`与`integration_regression_tests.log`。修改前远程源码备份在`integration_source_before/`；本地/远程原始hash一致。

最终远程回归全部通过：关键点7项、原MASK11项、像素有效性9项、DET残差注入6项、动作时序3项，共36项，另有上述真实双相机联调。配置往返测试首次错误地使用具体子类加载带type字段的配置，改为项目现有PreTrainedConfig注册入口后通过；没有修改通用配置解析器。

## 尚未完成与下一步

- 目前只有一个episode的3个可见点和1个遮挡标记，远不足以开展100k训练；其他帧不是遮挡真值，只是未标注。
- 下一步选取训练集多个episode的不同阶段与遮挡样例进行标注，另留独立开发审核样例；固定10个最终验证episode不用于调参。
- 标签审查后做小规模DET-NM与关键点辅助对照，并检查动作时序一致性和参考点误差。新增解码器会消耗随机数，不能仅凭相同seed假定共有网络初始化相同；对照应显式对齐共有权重。
- 暂不把SAM掩码中心批量作为真值，不用插值猜遮挡中心；下一步训练是否有效由独立验证决定。
