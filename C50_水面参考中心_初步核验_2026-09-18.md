# 水面参考中心：三帧标注与候选核验

任务范围：用户确认始终抓取装水杯。第三辅助监督方向调整为用户定义的可见水面参考中心，暂不要求模糊杯壁完整分割；不将点坐标直接变成夹爪抓取坐标。未实现/启动新关键点训练，DET注入不改动。

三张标记图保持原始640×480尺寸，提取各图唯一红点的像素质心。该提取识别的是人工画的红点，不是自动识别水面。低画质下尚不能独立确认可见区域属于物理水面还是杯底投影，保留用户定义，不声称三维定位真值。

| ep20帧 | 用户红点坐标 x,y | 现有SAM追踪掩码中心与标记距离 |
| --- | --- | --- |
| 0 | 147.5,263.5 | 1.77px |
| 538 | 137.5,248.5 | 2.89px |
| 717 | 145.0,249.3 | 5.21px |

三点均位于杯子CVAT检测框内，但相对检测框中心有水平偏移约11–18像素，不可把水面参考点当作检测框中心或实际抓取中心。538是已有轮廓追踪的种子，非独立验证。仅一个episode三帧不足以证明跨位置/姿态稳定。

## 重要反例

全718帧掩码中心的相邻位移：中位数0.674px、p95=2.948px，最大20.457px，出现在205帧。位移通常包含真实运动，不能直接作为误差。

但204→205原图已查看，杯子外观/位置近乎不变；对应CVAT框中心位移仅0.085px。这支持追踪中心在遮挡附近发生切换的疑点，而不是把20.5px全部解释成真实杯子移动。CVAT框也是参考，不代替人工点真值。此前无面积突跳仍可能有中心跳变，因此不直接将整段SAM中心当作训练标签，也不通过平滑掩盖问题。

## 下一步

先核查205帧：若水面参考中心可见，用一个红点标出；若被夹爪遮挡或看不清，直接声明不可见/不确定，不猜点。下一候选应显式保存点有效性，遮挡不确定帧不计算关键点损失。

再在少量不同episode/姿态验证参考点定义和标注一致性；通过后才实现关键点/热图辅助监督及短训。不将高斯点热图称为杯体分割真值，也不继续混合水面/杯壁mask目标。像素级忽略机制保留，不代表该关键点分支已经实现或准备完成。

远程及本地记录：`outputs/water_center_pilot_20260918/annotations.json`、`candidate_assessment.json`；标记副本在远程 `marked/`，原用户本地文件不覆盖。

工具：`scripts/extract_c50_water_points.py`、`scripts/analyze_c50_water_center_candidate.py`。正式数据、训练统计、checkpoint未更改，没有全量水面标签生产或训练启动。
