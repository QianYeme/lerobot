# episode 20 全流程轮廓种子追踪

采用用户认可的第 538 帧近似轮廓，保留种子掩码、SAM2 向前/向后追踪覆盖第 0–717 帧。未重新分割种子，不硬裁掩码，不替换正式标签、不开始训练。

## 实际结果

- 718 帧生成完整；空掩码 0，>50% 相邻面积突跳 0。
- 平均框外掩码面积占比 0.2126%，只是质量代理，不是像素准确率。
- 生成并查看 15 个对照帧：0、120、137、187、188、240、360、400、480、538、541、543、547、600、717。
- 种子附近前端背景溢出收敛，538–547 的覆盖较一致。抽样未见此前的大面积框下溢出，但不能据此证明所有帧都正确保留下部空隙或完全排除夹爪。
- 未遮挡/松夹姿态的开头及末尾，绿色似乎偏向杯体内侧，透明外沿存在疑似漏分；第 187/188 帧夹爪附近也需要语义核查。缺乏像素真值，以上是视觉疑点，不报告准确率，也不自动将这些帧标无效。

## 判定与下一步

全帧技术生成和面积稳定性检查通过，标签语义质量尚未通过，因此不晋升正式训练集。

下一步先请用户在第 0 帧原图圈出完整可见杯体外边界，与已认可的抓持姿态第 538 帧形成两个不同外观的轮廓锚点。两者都在训练 episode20，不涉及固定验证集。依据两个锚点追踪并检查中间外观变化；不要求逐帧人工标注。轮廓仍是提示，不直接宣布精确像素真值。

结果：`outputs/mask_inject_phase3_20260918/sam_contour_ep020_full/`，包含 NPZ、15 张对照、解码 JPEG 与 `results.json`。本地额外下载第0/717帧未标注 JPEG 便于人工核查；目录中的 input_frames 都是原始视频抽帧，不是涂色图。

复现命令（已存在输出会拒绝覆盖，应另选目录）：

```bash
cd /root/autodl-tmp/lerobot/lerobot-main
PYTHONPATH=/root/autodl-tmp/c50-mask-env/sam2-minimal:src \
  /root/autodl-tmp/c50-mask-env/bin/python scripts/check_c50_sam_contour.py \
  --start-frame 0 --end-frame 717 --modes contour_seed_track \
  --output outputs/mask_inject_phase3_20260918/sam_contour_ep020_full
```

轮廓回归 2 项通过；错误窗口不含种子538时在输出创建前拒绝。运行已经结束；本次启动命令的嵌套引号在 PowerShell/SSH 传递中失效，实际在保留的 SSH 会话前台完成，而非持久 screen，不将不存在的 screen 会话记作成功。
