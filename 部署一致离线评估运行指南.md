# C50 部署一致离线评估：主臂夹爪状态置零

准备日期：2026-09-16。脚本已完成远程静态检查、直接合成断言与 DET 两帧冒烟。全量评估已启动，运行状态见 `工作日志.md`；在正式 JSON 校验完成之前，不给出三模型全量结论。

## 1. 实验范围

在服务器项目 `/root/autodl-tmp/lerobot/lerobot-main` 中运行，不连接机械臂，不改变 checkpoint、训练数据或训练设置。

模型为 `C50_ACT_s1000`、`C50_DET_s1000`、`C50_INJECT_s1000` 的 `checkpoints/100000/pretrained_model`；验证 episodes 固定为 `7,11,13,14,16,27,35,38,40,43`，预期共 7180 帧。

每帧两次推理：原输入与 `master_gripper.pos` **原始状态置为 0 后再通过 checkpoint 保存的 preprocessor**。图像、其余状态、动作标签均保持一致，脚本逐 batch 检查。ACTDet 仅禁用不参与动作推理的标注加载，保留检测模块和特征注入配置。

只评估预测动作块的第 0 步（h0）。当前三个 checkpoint 均保存 `n_action_steps=1`、无 temporal ensemble，所以 h0 对应当前每次实际选择的动作；本实验不执行 `robot.send_action`，不模拟位移钳位。

这是 **teacher-forced 离线评估**：其余状态和腕部图像仍来自演示，不来自置零策略滚动执行。它能测量对主臂夹爪信号的依赖，但不能替代闭环真机成功率，不能单独定位启动抖动原因。

## 2. 将以下文件放到服务器相同相对路径

```text
src/lerobot/scripts/eval_master_gripper_ablation.py
scripts/prepare_c50_phase_review.py
scripts/run_master_gripper_eval.sh
tests/scripts/test_master_gripper_ablation.py
部署一致离线评估运行指南.md
```

这些文件随评估代码提交至 GitHub 的 `det` 分支。服务器有网络加速配置时先加载它，再以快进方式同步；远程已有改动应先保留，不执行 hard reset：

```bash
cd /root/autodl-tmp/lerobot/lerobot-main
source /etc/network_turbo >/dev/null
git fetch origin det
git merge --ff-only origin/det
```

也可以通过现有 SSH/SCP 同步相同相对路径。保留 LF 换行（Bash 文件不能使用 CRLF）。

## 3. 环境与静态检查

在服务器 Bash 终端执行：

```bash
cd /root/autodl-tmp/lerobot/lerobot-main
export PROJECT="$PWD"
export DATA_ROOT="$PROJECT/数据集/formal1_C"
export PYTHON_BIN=/root/miniconda3/bin/python
export PYTHONNOUSERSITE=1
export PYTHONPATH="$PROJECT/src${PYTHONPATH:+:$PYTHONPATH}"
export OUTPUT_ROOT="$PROJECT/outputs/formal1_C50_phase1/master_gripper_ablation_100k"
export PHASE_FILE="$OUTPUT_ROOT/phase_review/phase_annotations.json"

test -f "$DATA_ROOT/meta/info.json"
for MODEL in C50_ACT_s1000 C50_DET_s1000 C50_INJECT_s1000; do
  test -f "outputs/train/$MODEL/checkpoints/100000/pretrained_model/model.safetensors" || exit 1
done
"$PYTHON_BIN" -c 'import torch, av, pyarrow, PIL; print("CUDA:", torch.cuda.is_available())'
"$PYTHON_BIN" -m py_compile src/lerobot/scripts/eval_master_gripper_ablation.py scripts/prepare_c50_phase_review.py
bash -n scripts/run_master_gripper_eval.sh
```

`PYTHON_BIN` 必须指向已能运行原项目评估的环境。若不使用 `/root/miniconda3/bin/python`，替换该值。不要为了这个实验直接升级整套依赖。

若环境已有 pytest，可执行纯合成单测（不加载 checkpoint、不解码视频）：

```bash
"$PYTHON_BIN" -m pytest -q tests/scripts/test_master_gripper_ablation.py
```

没有 pytest 时，本命令会报缺少模块；这是测试尚未执行，不是测试通过。

## 4. 核验并填写阶段边界

```bash
"$PYTHON_BIN" scripts/prepare_c50_phase_review.py \
  --dataset-root "$DATA_ROOT" --output "$OUTPUT_ROOT/phase_review"
```

生成十张 `episode_XXX.jpg`：每隔 45 帧（当前 1.5 秒）抽样，标注 episode 内帧号；`action_traces.json` 每 15 帧记录 6 维动作。还生成 `phase_annotations.json`，初始全部标为 `unknown`、`reviewed=false`。重新生成总览不会覆盖已有人工阶段标注。

先看总览和轨迹，再播放原视频确认边界附近的动作。当前验证集均来自 top 视频 `videos/observation.images.top/chunk-000/file-000.mp4`；某 episode 视频内起始秒数由 `meta/episodes/chunk-000/file-000.parquet` 的 `videos/observation.images.top/from_timestamp` 给出。视频内时间 = 该起始秒数 + episode 内帧号 / 30。腕部视频可辅助判断夹持和释放。

允许的阶段：

| phase | 定义 |
|---|---|
| `approach` | 初始化、开爪及接近，直到开始实际夹持目标 |
| `grasp_transport` | 夹持、提起和携带，直到开始放置下降 |
| `place` | 放置下降、落台及释放 |
| `other` | 释放后的撤回、复位或无关等待 |
| `unknown` | 不能可靠判断，或边界过渡区 |

不要根据夹爪开合阈值自动声称抓取或放下成功。若没有对应动作阶段，不强行补一个。阶段是演示的时间标注，不是预测策略的阶段或成功判定。

在 JSON 中逐集修改 `segments`。所有区间使用 **episode 内帧号 `[start, stop)`**，包含 start、不包含 stop；不得重叠、不得越过该集长度。完成视频核验后将该集 `reviewed` 改为 `true`，在 `notes` 中记录边界依据；未覆盖帧自动归为 unknown。

例如：若某个经核验的 episode 接近为帧 0–99，则该段写成 `{"start": 0, "stop": 100, "phase": "approach"}`。这是格式示例，不是当前数据的真实边界。

未填写阶段仍可计算整体误差，但所有帧会落入 unknown，**不能声称已完成分阶段结论**。脚本拒绝 `reviewed=false` 的非 unknown 标签。

本次正式运行使用 `phase_review/core_phase_annotations.json`，由 top/腕部带帧号图像及动作轨迹核验保守阶段核心区间。覆盖 approach 1755、grasp_transport 855、place 630、other 985 帧，其余 2955 帧留作 unknown；这不是逐帧精确事件标注。复用本轮协议时先设置：

```bash
export PHASE_FILE="$OUTPUT_ROOT/phase_review/core_phase_annotations.json"
```

各正式结果 JSON 内嵌了实际使用的完整阶段标注与 SHA-256。unknown 仍计入整体指标，不能只用核心区间误差宣称全任务性能。

## 5. 两个 batch 冒烟评估

单独输出，不混入正式结果；每 batch 一帧，只检查加载、配对及输出链路，不用于模型排序：

```bash
"$PYTHON_BIN" -u -m lerobot.scripts.eval_master_gripper_ablation \
  --checkpoint outputs/train/C50_DET_s1000/checkpoints/100000/pretrained_model \
  --dataset.repo_id QYyyyyyyy/formal1_C --dataset.root "$DATA_ROOT" \
  --episodes 7,11,13,14,16,27,35,38,40,43 \
  --phase-file "$PHASE_FILE" --batch-size 1 --max-batches 2 \
  --output "$OUTPUT_ROOT/smoke_DET_h0_master_zero.json"
```

通过条件：无异常，JSON `frames=2`、`complete=false`，CSV 有两行预测；`protocol.raw_zero_before_preprocessor=true`。`master_zero_normalized_min_max` 通常不是 `[0,0]`，因为原始 0 经归一化后不等于归一化 0。

输出已存在时脚本拒绝覆盖。重试应改用一个新输出路径，保留错误现场和既有结果。

## 6. 三模型全量运行（内部也串行）

确认阶段标注和冒烟检查后执行：

```bash
screen -dmS C50_master_gripper_eval bash scripts/run_master_gripper_eval.sh all 8
screen -ls
screen -r C50_master_gripper_eval
```

按 Ctrl+A 再按 D 脱离 screen，SSH 断开不影响 screen 中的任务。脚本顺序为 ACT → DET → INJECT，任一组失败即停止，不生成全完成标记。不与已有视频评估争用解码资源；如显存不足，改用新 OUTPUT_ROOT，并用较小 batch 重试。

如只运行尚未完成的一组：

```bash
screen -dmS C50_master_DET bash scripts/run_master_gripper_eval.sh det 8
# 参数还可以是 act 或 inject
```

每模型输出：

```text
<模型>_h0_master_zero.json       # 整体、阶段、episode、关节统计
<模型>_h0_master_zero.csv        # 逐帧目标、原输入预测、置零预测（反归一化原始单位）
<模型>_h0_master_zero.log        # 进度或异常
<模型>_evaluator.py             # 实际使用的脚本快照
<模型>_code_commit.txt          # 代码提交
<模型>_tracked_changes.patch    # 已跟踪文件的差异
<模型>_command.sh               # 本次命令
<模型>.done                    # 该模型退出码为 0
complete.done                  # all 模式三组均成功时生成
```

标记文件不能替代 JSON 校验。正式 JSON 必须 `complete=true`、`frames=expected_frames=7180`；CSV 需含 7180 行数据，不能存在重复 episode/frame 身份。

## 7. 解读结果

`overall.normalized.by_group` 及 `by_phase.<阶段>.normalized.by_group` 中分别列出 arm、gripper、all_unweighted：

- `original_l1`：原输入预测对真实动作的误差；
- `zero_master_l1`：部署一致置零预测对真实动作的误差；
- `error_increase`：置零误差减原输入误差；正值表示恶化；
- `zero_vs_original_delta_l1`：两组预测变化幅度，不等于误差恶化。

每阶段同时报告有效帧数；零帧阶段没有 L1，不按误差 0 解读。默认全程都是 **未加夹爪 ×3 权重的平均绝对误差**，不能直接与旧全 100 步、带权重的 `inference_l1_loss` 比绝对值。原始单位逐关节统计在 `raw_units_by_action_dim`，不把不同关节的原始单位混成一个任务距离。

优先看夹爪在 grasp_transport/place 是否明显退化、arm 是否同步变化，以及 approach 初期是否受影响。若明显依赖主臂指令，下一轮训练应处理部署不可获取的输入；不要为了让旧模型跑起来而在真机伪造该标签。本实验本身不会修复旧 checkpoint。
