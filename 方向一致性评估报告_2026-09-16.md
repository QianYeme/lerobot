# 方向一致性评估报告（2026-09-16）

## 1. 评估目标

上一轮视觉敏感性评估证明：三个 C50 模型（ACT / DET / INJECT，100k checkpoint）对 top 图像扰动均有显著、跨 seed 稳定的响应。但它回答的是"会不会变"，没有回答"变得对不对"。

本轮回答最终优先级清单第一条：**杯子的左右位置变化与 shoulder_pan 输出方向是否一致**——即模型对视觉的敏感是朝正确方向驱动关节，还是只是无方向的噪声式响应。

## 2. 协议

- 脚本：`src/lerobot/scripts/eval_pan_direction_consistency.py`（新增）；
- checkpoint：三个模型的 100k checkpoint（与上一轮相同）；
- episodes：`7,11,13,14,16,27,35,38,40,43`；seed `1000`；
- 每模型 100 batches × 8 = 800 frames，比较动作块前 10 步的均值，归一化动作空间；
- 杯子中心 x：来自 top 相机 CVAT 标注 XML（每帧 box 中心，共 800/800 帧有标注）；
- 三个量（均为前 10 步均值）：

| 记号 | 含义 |
|---|---|
| `gt_pan` | 数据集真值动作块的 shoulder_pan |
| `pred_pan` | 原图推理的 shoulder_pan |
| `swap_pan` | 保持状态不变、换入 batch 内相邻样本的 top 图像后的 shoulder_pan |

- **观测回归**：`pan ~ cup_x`（真值与预测分别回归），斜率符号即"正确方向"的参考；
- **反事实回归（关键）**：`Δpan = swap_pan − pred_pan` 对 `Δcup_x = cup_x(换入图像) − cup_x(本帧)` 回归。状态输入不变的条件下，**不看图像的模型此斜率必然为 0**，因此它干净地隔离出"视觉通路把 pan 朝哪个方向推"；
- **方向一致率**：`|Δcup_x| ≥ 25 px` 的交换对中，`Δpan` 方向与 GT 斜率蕴含方向一致的比例（50% = 瞎猜）。

## 3. 结果

### 3.1 主表（800 frames / model，方向一致率 n=682）

| Model | GT 斜率（参考） | 观测斜率 pred | 反事实斜率 swap | swap / GT | 方向一致率 | Δpan~Δcup_x 相关 r |
|---|---:|---:|---:|---:|---:|---:|
| C50_ACT_s1000 | 0.006528 | 0.006658 | 0.003747 | 0.57× | 93.5% | 0.884 |
| C50_DET_s1000 | 0.006528 | 0.006381 | 0.003317 | 0.51× | 95.9% | 0.932 |
| C50_INJECT_s1000 | 0.006528 | 0.006389 | **0.006314** | **0.97×** | **96.8%** | **0.948** |

斜率单位：归一化 pan / 像素。跨 episode Spearman：GT `0.988`，三模型 pred 均为 `0.976`。

换入图像中杯子偏右 / 偏左时的平均 pan 响应（大位移对）：

| Model | 换入杯位偏右 Δpan | 换入杯位偏左 Δpan |
|---|---:|---:|
| ACT | +0.780 | −0.777 |
| DET | +0.768 | −0.599 |
| INJECT | **+1.278** | **−1.273** |

### 3.2 按位移幅度分箱的方向一致率

| Model | 25–100 px | 100–200 px | >200 px |
|---|---:|---:|---:|
| ACT | 0.856 (n=216) | 0.955 (n=112) | 0.977 (n=354) |
| DET | 0.898 (n=216) | 0.991 (n=112) | 0.986 (n=354) |
| INJECT | 0.912 (n=216) | 0.991 (n=112) | 0.994 (n=354) |

杯位差异越大，方向一致率越高（真实五方位杯位间距远大于 100 px，对应 >0.98 档）。

### 3.3 逐 episode（swap_pan 均值）

| episode | cup_x (px) | GT pan | ACT pred / swap | DET pred / swap | INJECT pred / swap |
|---:|---:|---:|---:|---:|---:|
| 7 | 340.8 | +0.1291 | +0.2062 / −0.1023 | +0.1603 / +0.0139 | +0.1920 / −0.2468 |
| 11 | 491.8 | +0.8759 | +0.8323 / +0.3557 | +0.7903 / +0.4421 | +0.7253 / −0.0830 |
| 13 | 220.2 | −0.7388 | −0.8154 / −0.6246 | −0.7240 / −0.5072 | −0.7051 / −0.4407 |
| 14 | 100.7 | −1.8053 | −1.7322 / −0.9372 | −1.6358 / −0.8793 | −1.6882 / −0.3121 |
| 16 | 437.1 | +0.3182 | +0.3289 / −0.0140 | +0.3285 / +0.0332 | +0.3461 / −0.3700 |
| 27 | 179.5 | −0.9356 | −1.0401 / −0.6581 | −0.9846 / −0.5399 | −1.0334 / −0.3103 |
| 35 | 495.9 | +1.1280 | +1.2216 / +0.3243 | +1.2170 / +0.4750 | +1.1978 / −0.3793 |
| 38 | 104.2 | −1.7976 | −1.8174 / −0.8828 | −1.7760 / −0.9008 | −1.7851 / −0.3886 |
| 40 | 432.7 | +0.5441 | +0.5732 / +0.0467 | +0.5456 / +0.1946 | +0.4982 / −0.2702 |
| 43 | 162.1 | −1.0713 | −1.1180 / −0.7190 | −1.0463 / −0.5969 | −1.0803 / −0.3062 |

观测列：三模型 pred 与 GT 几乎逐个 episode 吻合。swap 列：ACT/DET 的换图输出仍随本 episode 杯位变化（保留状态/先验成分）；**INJECT 的换图输出几乎不随 episode 变化（恒定 ≈ −0.3），说明其输出主要由图像决定**。

## 4. 结论

1. **方向一致性成立**：三个模型的 shoulder_pan 输出方向与杯子左右位置一致，不是无方向噪声。方向一致率 93.5–96.8%，大位移（>200 px）时 97.7–99.4%；反事实斜率与 GT 同号且显著。
2. **观测层面三模型都复现了 GT 的"杯位→pan"映射**（斜率 0.0064 vs GT 0.0065，跨 episode Spearman 0.976 vs 0.988）。注意该指标受状态输入与杯位的相关性影响，因果证据来自反事实一列。
3. **三者的差异在耦合增益，不在方向**：
   - ACT / DET 的换图响应约为 GT 斜率的 0.5–0.6 倍，状态与视觉混合决策；
   - **INJECT 的换图响应 ≈ 0.97 × GT 斜率，平均响应幅度（±1.28）约为 ACT（±0.78）的 1.65 倍**——它的 pan 指令几乎完全被图像（经 FCOS 特征注入）主导。
4. 结合上一轮结论（INJECT 干净图像误差最高、扰动退化最大、但检测头定位精度与 DET 相当），**INJECT 的问题可定性为"过耦合"：视觉→动作的增益过高**，遇到扰动或域外图像时动作被图像带走得更彻底。这不是"看不到杯子"，也不是方向错误，而是耦合强度失控。

## 5. 限制

- 反事实把样本 A 的状态与样本 B 的图像配对，测量的是响应方向与相对增益，不等于真机条件下的绝对误差；
- cup_x 为图像坐标，与 pan 的关系在观测范围内按线性近似；
- 单一 seed（1000）。上一轮三 seed 的聚合指标波动在 ±0.005 量级（相对值 ~2–5%），故单 seed 足以支撑方向性结论；
- 验证集为 10 个 episode（固定五方位杯位，实测 7 个不同 cup_x 取值）。

## 6. 对后续实验的意义

- **真机 A/B 优先级不变**：DET 的耦合增益（0.51×）与 ACT（0.57×）接近，但 DET 原图误差最低、扰动退化最小——"克制且准确"的视觉使用方式适合优先验证。
- **INJECT 若继续，建议先降低注入增益**（弱注入或可学习 gate）。本报告的"反事实斜率 / GT 斜率"可作为该改造的量化监控量：目标不是把它压到 0，而是从 0.97× 降到与 DET 相当的量级，再比较真机/离线表现。
- 对于论文：本分析给出了"视觉敏感性是被正确方向使用的"这一补充证据，可在响应章节与 swap/blank 敏感性表格并列呈现。

## 7. 复现

```bash
# 服务端（screen 内）：
bash scripts/run_pan_direction_eval.sh
# 产物：outputs/formal1_C50_phase1/pan_direction_100k/<model>_pan_direction.{json,log}
```

单模型等价命令：

```bash
/root/miniconda3/bin/python src/lerobot/scripts/eval_pan_direction_consistency.py \
  --checkpoint outputs/train/C50_DET_s1000/checkpoints/100000/pretrained_model \
  --dataset.repo_id QYyyyyyyy/formal1_C --dataset.root 数据集/formal1_C \
  --annotation-dir 数据集/formal1_C/annotations \
  --episodes 7,11,13,14,16,27,35,38,40,43 \
  --batch-size 8 --max-batches 100 --action-steps 10 --seed 1000 \
  --output outputs/formal1_C50_phase1/pan_direction_100k/C50_DET_s1000_pan_direction.json
```
