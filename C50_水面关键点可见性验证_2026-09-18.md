# 水面参考关键点：可见性与损失验证

## 本步结果

远程已实现稀疏标签读取、Gaussian热图生成和仅可见帧参与的热图MSE损失，5项测试通过。新增独立模块，不改现有ACT/DET/INJECT或MASK训练行为。还没有接入ACTDetPolicy正式训练入口，也没有启动训练。

标签：`outputs/water_center_pilot_20260918/keypoint_labels.json`。

| episode20帧 | 状态 | 原图坐标x,y |
|---|---|---|
| 0 | 可见，人工点 | 147.5,263.5 |
| 205 | 用户确认夹爪遮挡 | null，不猜测 |
| 538 | 可见，人工点 | 137.5,248.5 |
| 717 | 可见，人工点 | 144.95,249.27 |

未列出的帧是“未标注”，不是“已确认遮挡”；两者当前都不参与辅助损失。遮挡帧不删除，未来接入时继续保留其动作监督。Gaussian热图不是杯体分割掩码，也不是抓取坐标。目标是用户定义的可见水面参考点，不声称照片已独立证明水面与杯底投影的物理区别。

## 复现

```bash
cd /root/autodl-tmp/lerobot/lerobot-main
PYTHONPATH=src /root/miniconda3/bin/python tests/policies/act_det/test_water_keypoint.py -v
```

测试证据：`outputs/water_center_pilot_20260918/visibility_tests.log`。

验证四帧与未标注帧状态、热图坐标映射、非法标签拒绝、遮挡输出/目标即使为NaN也不影响损失、全遮挡辅助损失与梯度为0。独立动作标量仍有梯度，仅证明损失组合数学行为，不是完整ACT动作训练验证。

复用现有MaskDecoder做随机特征反传测试：可见帧的解码器与输入特征梯度非零，遮挡帧输出及输入特征梯度为0。没有使用真实图像拟合，没有证明定位改善。热图60×80、sigma=2网格像素是本次工程验证设置，非优化后最佳参数。

## 下一步

1. 补充跨episode、不同姿态/遮挡的少量人工参考点，分开拟合与独立审核样本，避免只验证种子帧。
2. 将关键点辅助损失接入可选训练分支，验证真实8状态、6动作、双相机批次及不依赖标签的推理。保留旧MASK功能，关键点目标不能冒充完整杯体分割。
3. 小规模试训比较独立验证点误差、辅助梯度与动作时序一致性，再决定是否生成更多标签及开展100k三组训练。

暂不把SAM掩码中心批量当真值，尤其205帧已确认遮挡；不插值猜中心，不宣布三组训练已准备完毕。
