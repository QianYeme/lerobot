# 采集脚本改动说明：力控抓取半满透明塑料杯

## 改动的目标

针对**力控抓取半满透明塑料杯**任务，采集脚本需要额外记录力觉和意图信号，用于训练"视觉-力感融合"和"混合动作头"模型。核心思路是：**透明物体纯视觉难以感知接触力，必须引入夹爪力觉反馈和人类操作者意图信号**。

---

## 总体架构：基线 vs 改动后

| 维度                   | 官方基线                 | 改动后                       |
| ---------------------- | ------------------------ | ---------------------------- |
| observation.state 维度 | 6（仅关节位置+夹爪位置） | 9（新增 3 维力感/意图信号）  |
| action 维度            | 6                        | 6（不变）                    |
| 力觉感知               | 无                       | 夹爪负载 + 电流读数          |
| 人类意图               | 无                       | 主控臂夹爪位置（位置差信号） |
| 同步读取模式           | 单寄存器逐一读取         | 多寄存器合并为一次同步读     |

---

## 详细改动

### 1. `src/lerobot/motors/feetech/feetech.py` — 新增 `sync_read_multi` 方法

**文件位置：** `src/lerobot/motors/feetech/feetech.py:332`

**改动内容：** 新增 `sync_read_multi()` 方法，支持在**一次同步读（Sync Read）事务**中从舵机一次性拉取多个寄存器的数据块。

**原官方做法：**

- 只调用 `sync_read("Present_Position")` 读取位置，每次只读一个寄存器
- 如果需要同时拿到位置、负载、电流，需要三轮独立通信，造成时间戳错位

**改动后：**

- 一次 Sync Read 拉取连续的寄存器块（start_addr 到 end_addr），在本地位拆分解码
- 同时获取 `Present_Position`（位置）、`Present_Load`（负载）、`Present_Current`（电流）三个寄存器值

**对力控抓取半满透明塑料杯的作用：**

- **时间戳严格对齐**：位置和力觉信号来自同一个通信事务，避免因通信延迟导致的力-位置失配
- **高帧率可用**：一次通信替代三次通信，同步读取开销不变但数据量翻倍，不拖慢 30fps 采集
- 这对训练力控模型至关重要——如果模型看到的"位置"和"受力"不在同一时刻，会学到错误的力-位映射关系

```python
# 接口签名
def sync_read_multi(
    self,
    data_names: list[str],  # 如 ["Present_Position", "Present_Load", "Present_Current"]
    motors: NameOrID | Sequence[NameOrID] | None = None,
    *,
    normalize: bool = True,
    num_retry: int = 0,
) -> dict[str, dict[str, Value]]:
```

---

### 2. `src/lerobot/motors/feetech/tables.py` — 新增 `Present_Current` 编码

**文件位置：** `src/lerobot/motors/feetech/tables.py:216`

**改动内容：** 在 `STS_SMS_SERIES_ENCODINGS_TABLE` 中新增一行：

```python
"Present_Current": 10,
```

**原因：** 飞特舵机（STS3215 等）的电流寄存器返回 1 字节（0-255），需解码为 -1000~1000 范围（单位 1mA）。之前只定义了 Present_Position（15 位）和 Present_Load（10 位）的解码，缺少电流的解码定义。

**对力控抓取半满透明塑料杯的作用：**

- **提供物理真实的力觉信号**：Load 是基于 PWM 估算的扭矩比例，可能有非线性死区；而 Current（电流，mA）是真实的物理电流反馈，与夹爪受力呈严格正相关
- **信噪比对比**：训练时把 Load 和 Current 都记录下来，后期根据信噪比（SNR）选择更可靠的信号作为力控特征
- 透明塑料杯表面光滑，滑动摩擦小，需要精确感知初始接触点——电流信号的线性度比 Load 更好，能更早检测到微小的接触力

---

### 3. `src/lerobot/robots/so_follower/so_follower.py` — 扩展观测特征

**文件位置：** `src/lerobot/robots/so_follower/so_follower.py`

**改动 3a：新增 `_gripper_force_ft` 属性（第 69-75 行）**

```python
@property
def _gripper_force_ft(self) -> dict[str, type]:
    return {
        "gripper.load": float,       # 从动臂夹爪负载（力觉信号 A）
        "gripper.curr": float,       # 从动臂夹爪电流（力觉信号 B）
        "master_gripper.pos": float, # 主控臂夹爪位置（人类意图信号）
    }
```

并将 `observation_features` 从 `{**self._motors_ft, **self._cameras_ft}` 改为 `{**self._motors_ft, **self._gripper_force_ft, **self._cameras_ft}`。

**改动 3b：重写 `get_observation()` 方法（第 186-210 行）**

**原官方做法：**

- 一次性用 `sync_read("Present_Position")` 读取所有电机（含夹爪）的位置
- 只记录 `{motor}.pos`

**改动后：**

1. 分别读取手臂电机（arm_motors）和夹爪（gripper）的遥测数据
2. 手臂电机沿用 `sync_read("Present_Position")` 拿位置
3. 夹爪使用新的 `sync_read_multi` **一次性拿 3 个值**：位置、负载、电流
4. 预留 `master_gripper.pos = 0.0` 占位，实际值由 `lerobot_record.py` 在获取到 teleop 动作后填入

**对力控抓取半满透明塑料杯的作用：**

- **gripper.load / gripper.curr**：模型可以"感受"到夹爪是否接触到杯壁，以及用了多大的力
  - 当夹爪空载闭合时，两个信号近似 0
  - 当夹爪碰到透明塑料杯并持续挤压时，两个信号急剧上升
  - 这解决了**透明物体视觉不可见接触力**的核心难题
- **master_gripper.pos**：记录了人类操作者捏遥控器的程度，是"期望握力"的代理信号
  - 从动臂实际位置 vs 主控臂位置之差（ΔP），等价于人类想施加的力控刚度
  - ΔP 信号通常比直接读舵机电流更平滑、更干净，是理想级训练标签

---

### 4. `src/lerobot/scripts/lerobot_record.py` — 采集循环的观测帧构建逻辑

**文件位置：** `src/lerobot/scripts/lerobot_record.py`

**改动内容：** 调整 `build_dataset_frame` 的调用时机和 `master_gripper.pos` 的赋值。

**原官方做法：**

- `observation_frame` 在记录循环开头构建一次（policy 和 dataset 两种情况共用）
- teleop 分支中不额外写 `master_gripper.pos`

**改动后：**

1. **将 `build_dataset_frame` 调用移到 policy/teleop 各自的分支内**（而非开头统一构建），因为 teleop 需要先拿到 `act` 才能填入 `master_gripper.pos`

2. **在 teleop 分支中注入 `master_gripper.pos`**：

   ```python
   if "gripper.pos" in act:
       obs_processed["master_gripper.pos"] = act["gripper.pos"]
   ```

   将人类操作者捏的遥控夹爪开口大小（`act["gripper.pos"]`）赋值给 `master_gripper.pos`

3. **teleop 赋值后再构建 `observation_frame`**，确保数据集记录的 observation 包含完整的人类意图信号

**对力控抓取半满透明塑料杯的作用：**

- 主控臂夹爪位置是**唯一的监督信号来源**——模型需要通过这个信号学习人类施加的力控策略
- 对于半满杯子，人类操作者会根据经验在不同阶段施加不同力度（刚接触轻捏、提起时用力捏紧防止滑落），这些策略都编码在主控臂/从动臂位置差中
- 在记录 loop 中注入而非在 `get_observation()` 中读取，是因为主控臂的真实位置只有 teleop 设备知道（不在舵机总线上）

---

## 数据变化示例

改动后的观测向量（来自 `info.json`）：

```
observation.state（9 维）:
  [0] shoulder_pan.pos     — 关节 1 位置
  [1] shoulder_lift.pos    — 关节 2 位置
  [2] elbow_flex.pos       — 关节 3 位置
  [3] wrist_flex.pos       — 关节 4 位置
  [4] wrist_roll.pos       — 关节 5 位置
  [5] gripper.pos          — 从动臂夹爪实际位置  [原有]
  [6] gripper.load         — 夹爪负载（力觉 A）  [新增]
  [7] gripper.curr         — 夹爪电流（力觉 B）  [新增]
  [8] master_gripper.pos   — 主控臂夹爪位置      [新增]

action（6 维，不变）:
  [0..5] 六个关节的目标位置（含 gripper.pos）
```

采集数据统计：40 个 episode，共 28691 帧，30fps。

---

## 对模型架构创新的支撑

| 架构创新                                       | 依赖的扩展信号                           | 用途                                                                    |
| ---------------------------------------------- | ---------------------------------------- | ----------------------------------------------------------------------- |
| **Mask-Guided Perception**（掩膜引导感知）     | 无需力感信号                             | 纯视觉路线，SAM 2 生成 Mask                                             |
| **Visual-Force Fusion**（视觉-力感融合）       | `gripper.load` / `gripper.curr`          | 将归一化后的力感信号作为额外 token 与视觉特征一起送 Transformer Encoder |
| **Hybrid Action Head**（混合动作头）           | `master_gripper.pos - gripper.pos`（ΔP） | 训练时作为目标刚度标签；部署时输出扭矩限制实现软停                      |
| **Spatio-Temporal Backbone**（时空动力学骨干） | 无额外需求                               | 纯视觉动态路线，重点在时间同步精度                                      |

---

## 注意事项

1. **`master_gripper.pos` 目前硬编码为 0.0**：在 `so_follower.py:200` 处预留了占位值，实际值由采集 Loop 中的 teleop 动作覆盖。如果后续支持从主控臂舵机直接读取（如双总线架构），需要在此处实现真实的读取逻辑。

2. **`Present_Current` 寄存器读取长度**：飞特 STS/SMS 系列电流寄存器为 1 字节（length=2 in register table），解码时使用 10 位编码（与 Present_Load 一致）。如果发现电流值范围不对，需检查寄存器表的地址配置。

3. **`sync_read_multi` 的块大小限制**：飞特 Sync Read 指令的最大 Block 大小受协议限制（通常 128 字节）。当前拉取位置+负载+电流共约 8 字节，远未触达上限。
