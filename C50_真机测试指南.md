# C50 真机测试指南（round 1）

> 适用模型：`C50_ACT_s1000` / `C50_DET_s1000` / `C50_INJECT_s1000`（formal1_C，100k checkpoint）
> 运行机：真机所在的 Ubuntu 侧（`/home/lyj/lerobot`，conda 环境 `lerobotnn`）
> 脚本：`src/lerobot/scripts/control_act_det.py`（本提交新增动作 CSV 日志 + 相机确认防暖机误判）

---

## 0. 前置检查（逐项过，任何一项不过就别开测）

| # | 检查 | 命令 / 做法 | 通过标准 |
|---|---|---|---|
| 1 | GPU | `nvidia-smi`；`python -c "import torch; print(torch.cuda.is_available())"` | 正常输出；`True`（否则控制循环达不到 30Hz） |
| 2 | 代码版本 | `md5sum src/lerobot/scripts/control_act_det.py` | = `9cdb31cc9c6933225556205a1f219188` |
| 3 | 数据集 | `ls 数据集/formal1_C/meta/info.json` | 存在（完整数据集含 meta/annotations，共约 411MB） |
| 4 | 三个模型 | `du -sh outputs/train/C50_*/checkpoints/100000/pretrained_model` | ≈198M / 225M / 226M |
| 5 | 干跑加载（不接机器人） | 见 §0.1 | 三个模型都打印 `OK`，无异常 |
| 6 | 串口 / 相机 | `lerobot-find-port`、`lerobot-find-cameras` | 记下串口；确认 top=桌面全局、gripper=腕部 |
| 7 | 相机语义人工确认 | 跑 §1 命令，脚本会存 `outputs/camera_check/*.jpg` 并等回车 | 画面语义正确（已改为连读 10 帧取最后一张，避免暖机瞬态误判） |
| 8 | 安全 | 急停手边；leader 臂移出工作区；从动臂摆到演示起始位姿附近 | — |

### 0.1 干跑加载

```bash
cd /home/lyj/lerobot && python - <<'PY'
from pathlib import Path
from lerobot.configs.policies import PreTrainedConfig
from lerobot.datasets.dataset_metadata import LeRobotDatasetMetadata
from lerobot.policies.factory import make_policy, make_pre_post_processors
meta = LeRobotDatasetMetadata("formal1_C", root="数据集/formal1_C")
for m in ["C50_ACT_s1000", "C50_DET_s1000", "C50_INJECT_s1000"]:
    ckpt = Path(f"outputs/train/{m}/checkpoints/100000/pretrained_model")
    cfg = PreTrainedConfig.from_pretrained(ckpt)
    make_policy(cfg, ds_meta=meta)
    make_pre_post_processors(policy_cfg=cfg, pretrained_path=str(ckpt))
    print("OK", m, cfg.type)
PY
```

（checkpoint 的 `config.json` 里 `annotation_dir` 是训练机路径，推理时自动禁用，不会报错。）

---

## 1. 运行命令

```bash
conda activate lerobotnn && cd /home/lyj/lerobot && mkdir -p logs
python src/lerobot/scripts/control_act_det.py \
  --policy.path outputs/train/C50_DET_s1000/checkpoints/100000/pretrained_model \
  --robot.type=so101_follower --robot.port=/dev/ttyACM0 --robot.id=nn \
  --robot.cameras='{top: {type: opencv, index_or_path: 4, width: 640, height: 480, fps: 30}, gripper: {type: opencv, index_or_path: 2, width: 640, height: 480, fps: 30}}' \
  --dataset.repo_id formal1_C \
  --dataset.root /home/lyj/lerobot/数据集/formal1_C \
  --dataset.single_task="Cup pick and place" \
  --dataset.num_episodes 10 --dataset.episode_time_s 60 --dataset.reset_time_s 30 \
  --dataset.fps 30 2>&1 | tee logs/C50_DET_log.txt
```

- `--robot.type` 用仓库注册名：`so101_follower`（SO-100 臂用 `so100_follower`）；旧文档的 `so_follower` 会报 unknown type。
- 相机 index 以 `lerobot-find-cameras` 为准（历史确认值：top=4 / gripper=2；重启后编号可能变，每次会话前确认）。
- 相机 fps / 分辨率必须与训练一致（640×480@30）——数据集视频就是这个规格。
- 换模型 = 换 `--policy.path` 重开会话；单次启动几十秒。

按键：`→` 提前结束当前段，`Esc` 结束整个会话。

---

## 2. 测试协议

### 2.1 杯位

- 用胶带在桌面贴 10 个点（覆盖视野中部、含左右两侧），**拍照留档**；三个模型复用同一批点（控制变量）。
- 若沿用《ACTDET 模型测试指南》的 C01–C30 表：本轮用 C01–C05 冒烟 + C06–C10 主跑；实测坐标以现场为准。
- 参考：formal1_C 训练数据的杯位覆盖 top 画面 x≈49–536px 的宽范围（90 集接近连续），所以网格布点不会"全是分布外"；但别超出你采集时覆盖的视觉范围。

### 2.2 阶段

| 阶段 | 做法 | 判定门 |
|---|---|---|
| A 探头 | 每模型 2 次短会话（`--dataset.num_episodes 3 --dataset.episode_time_s 15`）：手推杯子在视野内左右移动 | 肩 pan 跟随杯位方向；不跟随 → 停测查因 |
| B 冒烟 | 三模型 × 前 5 个杯位（共 15 段） | 抓取成功率明显为 0 → 停手诊断 |
| C 主跑 | 三模型 × 后 5 个杯位（共 15 段）；模型顺序轮换（DET→ACT→INJECT） | 初步对比，决定谁值得投 30 段 |

### 2.3 四阶段判定

| 阶段 | 标准 |
|---|---|
| 1 抓取 | 夹爪稳定夹住杯身且杯底离台 |
| 2 移动 | 平稳移向目标位置，水无大量洒出 |
| 3 放下 | 松开夹爪后杯子直立不倒 |
| 4 整体 | 1–3 全部通过 |

录像命名：`{模型}_{杯位}_run{n}.mp4`。重点保留 INJECT 的过冲 / 被图像带偏片段（过耦合假设的现实证据）。

---

## 3. 动作 CSV 日志（本提交新增）

每次会话自动写 `outputs/control_logs/<时间戳>_<模型名>.csv`，列：

| 列 | 含义 |
|---|---|
| `t_s` | 本段内时间（秒） |
| `episode` | 第几段 |
| `target.<关节>` | 策略输出目标（6 维，归一化反变换后） |
| `sent.<关节>` | 实际发送给从动臂的值（可能被 `max_relative_target` 钳位） |
| `gripper_actual` | 夹爪实测位置 |

用法：

- **探头定量**：把 `target.shoulder_pan.pos` 对 `t_s` 画图——手推杯子左→右时 pan 应同向变化。这是离线"方向一致性"（反事实斜率）结论的真机版同口径检查；
- **钳位诊断**：`target` 与 `sent` 的差 = 控制钳位量；
- **抓持判断**：`target.gripper.pos` 已闭合但 `gripper_actual` 不动 → 夹爪机械/负载问题；target 就没闭 → 策略问题；
- 与录像时间轴对齐（录像起点 ≈ 会话起点）后可逐段复核。

---

## 4. 记录与汇总

- 逐段判定表：`判定表_round1.csv`（列：阶段 / 模型 / 杯位 / 抓取 / 移动 / 放下 / 整体 / 失败模式 / 录像文件 / 备注）。
- 汇总口径：分阶段成功率 + 整体成功率。round 1 每模型 10 段 = 定性结论；论文主表需要每核心模型 30 段。
- 失败模式建议编码：抓空 / 提前闭爪 / 悬停不闭爪 / 撞倒 / 过冲 / 放偏 / 不追杯。

---

## 5. 常见问题

| 现象 | 处理 |
|---|---|
| `Control loop is running slower than 30 Hz` | 确认 GPU 空闲（`nvidia-smi`）、相机 fps=30；必要时降低相机分辨率 |
| 策略不动 / 不追杯 | 先看 `outputs/camera_check/*.jpg` 语义是否正确（"蒙眼"跑与坍缩无法区分）；再看 CSV 里 pan 是否随杯位变化 |
| 首条动作幅度大 | 每段开始把从动臂摆到演示起始位姿附近 |
| 抓取前悬停 / 不闭爪 | 看 CSV 的 target vs actual（见 §3） |
| 相机画面异常 | 已连读 10 帧仍异常 → 手动跑 `lerobot-find-cameras` 复核 index；画面发绿/发暗先等 2 秒（暖机瞬态） |
| 想边测边录数据 | 用 `lerobot-record --policy.path <checkpoint> --resume`（会追加进数据集，注意别污染训练集） |

---

## 6. 测完带回

`logs/*.log`、`outputs/control_logs/*.csv`、判定表、录像文件、`outputs/camera_check/`。
