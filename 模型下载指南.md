# 模型下载指南（Hugging Face）

15 个已训练模型已上传至 Hugging Face（用户 `QYyyyyyyy`，均为公开仓库）。本指南说明如何在 Ubuntu 上完整下载并放到正确位置，下载完成后可直接按《ACTDET_模型测试指南.md》进行测试。

---

## 1. 仓库清单与目标位置

每个仓库的内容 = 训练时 checkpoint 的 `pretrained_model/` 目录（`config.json` + `model.safetensors` + 预处理/后处理器文件），**不含 `training_state`（不能续训，只能测试，这正符合测试用途）**。

为让测试指南里的命令原样可用，下载目标目录复刻训练机的 `outputs/train` 结构：`outputs/train/<日期>/<运行目录>/checkpointsE<X>/last/pretrained_model/`。

| HF 仓库（`QYyyyyyyy/` 下） | 实验 | 配置说明 | 数据集 | 目标目录（lerobot-main 下） |
|---|---|---|---|---|
| `02-55-15_act_checkpointsE1` | E1 | 基线 ACT | formal_A | `outputs/train/2026-08-07/02-55-15_act/checkpointsE1/last/pretrained_model` |
| `02-56-09_act_det_checkpointsE2` | E2 | +检测 | formal_A | `outputs/train/2026-08-07/02-56-09_act_det/checkpointsE2/last/pretrained_model` |
| `16-25-39_act_det_checkpointsE3` | E3 | +检测+Mask引导 | formal_A | `outputs/train/2026-08-07/16-25-39_act_det/checkpointsE3/last/pretrained_model` |
| `16-26-04_act_det_checkpointsE3b` | E3b | 与 E3 同配置（重复训练） | formal_A | `outputs/train/2026-08-07/16-26-04_act_det/checkpointsE3b/last/pretrained_model` |
| `16-28-36_act_checkpointsE4` | E4 | 基线 ACT（力感） | formal1_B | `outputs/train/2026-08-07/16-28-36_act/checkpointsE4/last/pretrained_model` |
| `01-17-03_act_det_checkpointsE5` | E5 | +检测 | formal1_B | `outputs/train/2026-08-08/01-17-03_act_det/checkpointsE5/last/pretrained_model` |
| `01-17-21_act_det_checkpointsE6` | E6 | +检测+Mask引导 | formal1_B | `outputs/train/2026-08-08/01-17-21_act_det/checkpointsE6/last/pretrained_model` |
| `01-17-41_act_det_checkpointsE6b` | E6b | 与 E6 同配置（重复训练） | formal1_B | `outputs/train/2026-08-08/01-17-41_act_det/checkpointsE6b/last/pretrained_model` |
| `12-32-41_act_det_checkpointsE7` | E7 | +检测+FCOS注入 | formal1_B | `outputs/train/2026-08-08/12-32-41_act_det/checkpointsE7/last/pretrained_model` |
| `00-03-00_act_det_checkpointsE7b` | E7b | +检测+Mask引导+FCOS注入 | formal1_B | `outputs/train/2026-08-09/00-03-00_act_det/checkpointsE7b/last/pretrained_model` |
| `12-33-40_act_det_checkpointsE8` | E8 | +检测+Mask引导+Mask注入 | formal1_B | `outputs/train/2026-08-08/12-33-40_act_det/checkpointsE8/last/pretrained_model` |
| `12-34-49_act_det_checkpointsE9` | E9 | 全开（双注入） | formal1_B | `outputs/train/2026-08-08/12-34-49_act_det/checkpointsE9/last/pretrained_model` |
| `00-03-19_act_det_checkpointsE_A0` | E_A0 | 检测，**无增强**（aug_enable=false） | formal1_B | `outputs/train/2026-08-09/00-03-19_act_det/checkpointsE_A0/last/pretrained_model` |
| `00-03-39_act_det_checkpointsE_A1` | E_A1 | 检测，**仅遮挡** | formal1_B | `outputs/train/2026-08-09/00-03-39_act_det/checkpointsE_A1/last/pretrained_model` |
| `09-02-51_act_det_checkpointsE_A2` | E_A2 | 检测，**无遮挡**（噪声+色彩抖动） | formal1_B | `outputs/train/2026-08-09/09-02-51_act_det/checkpointsE_A2/last/pretrained_model` |

E_A0 / E_A1 / E_A2 是数据增强消融系列（对比检测分支上三种增强配置）。

---

## 2. 下载前准备（Ubuntu 执行一次）

```bash
# 1. 安装 huggingface 命令行（含 hf_transfer 多线程加速）
pip install -U "huggingface_hub[hf_transfer]"

# 2. 国内网络必须走镜像（直连 huggingface.co 大概率超时）
export HF_ENDPOINT=https://hf-mirror.com

# 3. 可选：开启多线程高速下载
export HF_HUB_ENABLE_HF_TRANSFER=1
```

- 仓库是公开的，**不需要登录**、不需要 token
- 建议把上面两个 `export` 写入 `~/.bashrc`，避免每次新终端都要重新设置：

```bash
echo 'export HF_ENDPOINT=https://hf-mirror.com' >> ~/.bashrc
echo 'export HF_HUB_ENABLE_HF_TRANSFER=1' >> ~/.bashrc
source ~/.bashrc
```

---

## 3. 单个模型下载示例（E9）

```bash
cd /root/autodl-tmp/lerobot/lerobot-main   # 或你本机的 lerobot-main 路径

huggingface-cli download QYyyyyyyy/12-34-49_act_det_checkpointsE9 \
    --local-dir outputs/train/2026-08-08/12-34-49_act_det/checkpointsE9/last/pretrained_model
```

---

## 4. 一键下载全部 15 个模型

```bash
#!/usr/bin/env bash
set -e
export HF_ENDPOINT=https://hf-mirror.com
export HF_HUB_ENABLE_HF_TRANSFER=1
cd /root/autodl-tmp/lerobot/lerobot-main   # ← 改成你的 lerobot-main 路径

dl() {  # $1=仓库名  $2=目标相对目录
    echo "==> $1"
    huggingface-cli download "QYyyyyyyy/$1" --local-dir "$2"
}

dl 02-55-15_act_checkpointsE1        outputs/train/2026-08-07/02-55-15_act/checkpointsE1/last/pretrained_model
dl 02-56-09_act_det_checkpointsE2    outputs/train/2026-08-07/02-56-09_act_det/checkpointsE2/last/pretrained_model
dl 16-25-39_act_det_checkpointsE3    outputs/train/2026-08-07/16-25-39_act_det/checkpointsE3/last/pretrained_model
dl 16-26-04_act_det_checkpointsE3b   outputs/train/2026-08-07/16-26-04_act_det/checkpointsE3b/last/pretrained_model
dl 16-28-36_act_checkpointsE4        outputs/train/2026-08-07/16-28-36_act/checkpointsE4/last/pretrained_model
dl 01-17-03_act_det_checkpointsE5    outputs/train/2026-08-08/01-17-03_act_det/checkpointsE5/last/pretrained_model
dl 01-17-21_act_det_checkpointsE6    outputs/train/2026-08-08/01-17-21_act_det/checkpointsE6/last/pretrained_model
dl 01-17-41_act_det_checkpointsE6b   outputs/train/2026-08-08/01-17-41_act_det/checkpointsE6b/last/pretrained_model
dl 12-32-41_act_det_checkpointsE7    outputs/train/2026-08-08/12-32-41_act_det/checkpointsE7/last/pretrained_model
dl 00-03-00_act_det_checkpointsE7b   outputs/train/2026-08-09/00-03-00_act_det/checkpointsE7b/last/pretrained_model
dl 12-33-40_act_det_checkpointsE8    outputs/train/2026-08-08/12-33-40_act_det/checkpointsE8/last/pretrained_model
dl 12-34-49_act_det_checkpointsE9    outputs/train/2026-08-08/12-34-49_act_det/checkpointsE9/last/pretrained_model
dl 00-03-19_act_det_checkpointsE_A0  outputs/train/2026-08-09/00-03-19_act_det/checkpointsE_A0/last/pretrained_model
dl 00-03-39_act_det_checkpointsE_A1  outputs/train/2026-08-09/00-03-39_act_det/checkpointsE_A1/last/pretrained_model
dl 09-02-51_act_det_checkpointsE_A2  outputs/train/2026-08-09/09-02-51_act_det/checkpointsE_A2/last/pretrained_model

echo "全部下载完成"
```

- 总下载量约 **3.3 GiB**（15 个模型，单个 200–237 MB）
- 断点续传：`huggingface-cli download` 中断后重跑同一条命令即可，已完成的文件自动跳过

---

## 5. 下载后校验

每个目标目录应包含 7 个文件：

```
config.json
model.safetensors            # 约 200–237 MB
train_config.json
policy_preprocessor.json
policy_preprocessor_step_3_normalizer_processor.safetensors
policy_postprocessor.json
policy_postprocessor_step_0_unnormalizer_processor.safetensors
```

批量校验命令（输出全部 OK 即成功）：

```bash
cd /root/autodl-tmp/lerobot/lerobot-main
for d in outputs/train/*/*/checkpointsE*/last/pretrained_model; do
    if [ -f "$d/model.safetensors" ] && [ -f "$d/config.json" ]; then
        echo "OK   $d"
    else
        echo "FAIL $d"
    fi
done
```

---

## 6. 注意事项

1. **数据集仍是测试的前置条件**：离线评估需要 `数据集/formal_A`、`数据集/formal1_B`（含标注）；真机测试至少需要数据集的 `meta/info.json` 与 `stats`（归一化统计量）。如果下载机不是原来的训练机，先把数据集一并拷贝过去
2. **config 里的 `annotation_dir` 是训练机绝对路径**（`/root/autodl-tmp/...`）。离线评估脚本自动推断数据集依赖它，若下载机路径不同，脚本会推断失败 → 显式传 `--dataset.repo_id` 和 `--dataset.root`（详见《ACTDET_模型测试指南.md》§2.2 / §5）
3. 仓库只含 `pretrained_model` 内容，**不含 `training_state`**，因此无法断点续训——测试足够用
4. 下载完成后按《ACTDET_模型测试指南.md》执行：离线评估 → 真机 4 阶段测试（核心 E1/E2/E9 × 30 次、其余 × 10 次）

---

## 7. 硬件说明（RTX 4060 显卡 + 16GB 内存）

| 场景 | 显存需求 | 结论 |
|------|----------|------|
| **真机测试**（control_act_det.py，batch=1 推理） | 峰值约 3–5 GB | ✅ 4060（8GB 显存）与 4060 Ti（16GB 显存）都轻松跑 |
| **离线评估**（默认 `--batch-size 8`） | 训练时 batch 8 接近 16GB 边界 | ⚠️ 8GB 显存版需 `--batch-size 2`；16GB 版可用 batch 8 |
| **系统内存 16GB** | 数据集懒加载 | ✅ 足够 |

- 离线评估的指标是逐帧加权平均，**与 batch size 无关**，改小 batch size 不影响结果，只影响速度
- 如果 Ubuntu 上没有 NVIDIA 显卡（只有核显），真机控制达不到 30Hz 目标帧率、离线评估极慢，不建议在此机器测试
