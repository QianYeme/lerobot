# C50 部署一致主臂夹爪置零评估报告

日期：2026-09-16。全量任务于北京时间 18:18:12 启动，18:51:56 完成，耗时约 33 分 44 秒。

## 1. 结论

固定 10 个验证 episode、三个 100k checkpoint 的成对第 0 步评估全部完成并通过独立复核。

- **ACT、DET 明显依赖部署时不可获得的主臂夹爪指令。** 原始 `master_gripper.pos` 置零后，夹爪误差分别变为原来的 6.47 倍、8.49 倍，两个模型均在全部 10 个 episode 上恶化。
- **INJECT 对该信号的依赖明显较小，但不是完全不依赖。** 夹爪误差增加 25.3%，总体误差增加 6.4%；7 个 episode 恶化，3 个改善。
- **模型的离线排序随输入条件改变。** 原输入下总体误差 DET < ACT < INJECT；部署一致置零后 INJECT < ACT < DET。INJECT 的置零夹爪误差最低，但机械臂误差仍高于 ACT、DET，不能直接宣布闭环最优。
- **该输入偏差不能单独解释启动抖动。** 每个验证 episode 的前 30 帧合计 300 帧，主臂夹爪原始值均值仅 0.3720；三模型置零后的预测变化很小。启动抖动仍需真机控制与状态日志定位。

这里的“部署一致”仅指修正主臂夹爪这一维输入，不表示整个离线实验已经等同于真机闭环。

## 2. 评估协议

| 项目 | 本次设置 |
|---|---|
| 模型 | C50_ACT_s1000、C50_DET_s1000、C50_INJECT_s1000 |
| checkpoint | 各模型 `checkpoints/100000/pretrained_model` |
| 数据集 | `QYyyyyyyy/formal1_C`，C50 固定验证集 |
| episode | 7、11、13、14、16、27、35、38、40、43 |
| 样本 | 每 episode 718 帧，每模型 7,180 帧、898 batches |
| 成对条件 | 原始输入 / 预处理前将原始 `master_gripper.pos` 置为 0 |
| 唯一干预 | 9 维 state 中第 8 索引；其余状态、图像、动作标签不变 |
| 处理器 | 沿用各 checkpoint 保存的 pre/postprocessor，不重新估计统计 |
| 执行步 | `predict_action_chunk` 的第 0 步；`n_action_steps=1`，无 temporal ensemble |
| 推理 | eval 模式、无梯度；batch=8；PyAV；num_workers=0 |
| 指标 | 归一化平均绝对误差；arm=5 关节、gripper=1 维，未加夹爪 ×3 权重 |
| 运行方式 | 远程 RTX 5090，screen 中 ACT → DET → INJECT 串行 |

原始 0 经保存的归一化器后为 **-1.124658**，不是归一化后的 0。

本次仍使用演示中的其余状态和相机图像，即 teacher-forced 离线评估，没有让机械臂滚动执行。

采集代码 `src/lerobot/scripts/lerobot_record.py:376` 将 `act["gripper.pos"]` 写入 `master_gripper.pos`；自主 follower 在 `src/lerobot/robots/so_follower/so_follower.py:201` 将它设为 0。本次完整 7,180 帧 CSV 再次确认：原始主臂夹爪信号与当前目标 `action.gripper.pos` **逐帧完全相等，最大差为 0**。这是当前动作标签泄漏，而不是普通的历史状态输入。

## 3. 总体结果：实际执行的第 0 步

以下为归一化 L1，越低越好。“原 → 零”表示原输入误差到置零输入误差。

| 模型 | arm：原 → 零 | arm 增幅 | gripper：原 → 零 | gripper 倍数 | 全 6 维：原 → 零 |
|---|---:|---:|---:|---:|---:|
| ACT | 0.084355 → 0.087760 | +4.0% | 0.089048 → 0.576549 | 6.47× | 0.085137 → 0.169225 |
| DET | 0.070536 → 0.086956 | +23.3% | 0.072558 → 0.616372 | 8.49× | 0.070873 → 0.175192 |
| INJECT | 0.100344 → 0.101275 | +0.9% | 0.143583 → 0.179937 | 1.25× | 0.107551 → 0.114385 |

全 6 维误差增幅分别为 ACT +98.8%、DET +147.2%、INJECT +6.4%。它们不是任务失败率，也不能与旧的全 100 步带权 `inference_l1_loss` 比绝对值。

夹爪反归一化后的误差与平均预测如下。单位为数据中的原始 `gripper.pos` 单位，不是毫米。

| 模型 | 夹爪原输入 MAE | 夹爪置零 MAE | 两条件预测差 MAE | 原输入预测均值 | 置零预测均值 |
|---|---:|---:|---:|---:|---:|
| ACT | 1.5954 | 10.3295 | 10.1217 | 23.7908 | 13.6713 |
| DET | 1.3000 | 11.0430 | 10.2620 | 23.2258 | 12.9644 |
| INJECT | 2.5725 | 3.2238 | 1.6714 | 23.2435 | 21.5726 |

共同目标夹爪均值为 23.8978。ACT、DET 置零后平均夹爪目标明显降低，但这不表示输出全为 0。预测变化幅度也不等于误差增量。

## 4. 接近、抓取搬运、放下阶段

阶段在完整结果生成前根据 top 总览、动作轨迹以及候选区间起点/中点/终点附近的 top 和腕部图像核验。采用保守核心区间，不是逐帧精确事件标注。抓取阶段包含抓握、抬起与搬运。

| 阶段 | 帧数 | ACT 夹爪：原 → 零 | DET 夹爪：原 → 零 | INJECT 夹爪：原 → 零 |
|---|---:|---:|---:|---:|
| 接近 approach | 1,755 | 0.112404 → 0.625849 | 0.097261 → 0.750630 | 0.202585 → 0.253821 |
| 抓取搬运 grasp_transport | 855 | 0.064286 → 0.251745 | 0.045509 → 0.240262 | 0.064449 → 0.070907 |
| 放下 place | 630 | 0.080909 → 0.473293 | 0.058959 → 0.465666 | 0.121760 → 0.181017 |
| 退回/复位 other | 985 | 0.017143 → 0.017048 | 0.016722 → 0.016759 | 0.011227 → 0.011411 |
| 未确定过渡区 unknown | 2,955 | 0.108044 → 0.849762 | 0.087224 → 0.877459 | 0.180210 → 0.223548 |

| 阶段 | ACT arm：原 → 零 | DET arm：原 → 零 | INJECT arm：原 → 零 |
|---|---:|---:|---:|
| 接近 | 0.109893 → 0.120950 | 0.102315 → 0.126074 | 0.124240 → 0.130593 |
| 抓取搬运 | 0.087947 → 0.092229 | 0.062458 → 0.082601 | 0.107882 → 0.108571 |
| 放下 | 0.083833 → 0.082779 | 0.067497 → 0.075085 | 0.097835 → 0.095981 |
| 退回/复位 | 0.039792 → 0.039791 | 0.038855 → 0.038932 | 0.037959 → 0.037969 |
| 未确定过渡区 | 0.083114 → 0.083807 | 0.065209 → 0.083522 | 0.105302 → 0.103982 |

ACT、DET 在三个核心任务阶段的夹爪误差均明显增大；INJECT 在抓取搬运核心段的退化最小，放下与接近仍有退化。DET 的 arm 也出现同步恶化，不能只修补夹爪输出而假定机械臂完全不受影响。

unknown 占全量约 41.2%，其结果计入总体，但不能强行归入某个阶段。三个任务核心段合计 3,240 帧，阶段结论只覆盖这些已标区间。other 的影响很小，不能用复位段替代真正抓取阶段。

## 5. episode 一致性与启动区间

下表为各 episode 的归一化夹爪误差增量（置零 − 原输入），正值表示恶化。

| episode | ACT | DET | INJECT |
|---|---:|---:|---:|
| 7 | +0.367186 | +0.373964 | +0.047942 |
| 11 | +0.445439 | +0.515552 | -0.041203 |
| 13 | +0.602696 | +0.639518 | +0.085951 |
| 14 | +0.669540 | +0.764803 | +0.092579 |
| 16 | +0.449616 | +0.539212 | +0.056218 |
| 27 | +0.483698 | +0.521815 | -0.024918 |
| 35 | +0.402340 | +0.468806 | -0.003615 |
| 38 | +0.512688 | +0.598032 | +0.065661 |
| 40 | +0.510626 | +0.556463 | +0.053628 |
| 43 | +0.431183 | +0.459972 | +0.031291 |

补充检查每个 episode 前 30 帧，共 300 帧。原始 master 均值为 0.372037、最大值为 7.470747，与整个验证集均值 23.897803 不同。

| 模型 | 初期 arm 误差：原 → 零 | 初期夹爪误差：原 → 零 | 初期 arm 预测变化 L1 | 初期夹爪预测变化 L1 |
|---|---:|---:|---:|---:|
| ACT | 0.102746 → 0.102728 | 0.023944 → 0.024244 | 0.000257 | 0.000368 |
| DET | 0.092532 → 0.091981 | 0.016386 → 0.018862 | 0.000650 | 0.002602 |
| INJECT | 0.082966 → 0.083061 | 0.030226 → 0.031956 | 0.000376 | 0.001735 |

这是演示初期的描述性检查，不是真机启动状态的重现。结果支持“输入偏差主要影响任务过程中的夹爪动作”，不支持“已找到几秒内初始抖动的唯一根因”。

## 6. 完成与复核证据

- 三份正式 JSON 均 `complete=true`、`frames=expected_frames=7180`、`batches=898`。
- 三份 CSV 各 7,180 行，episode/frame 身份唯一，完整覆盖 10 × 718；预测与目标均有限值；`master_zero_raw` 全为 0。
- 独立读取 CSV，从保存的 action 标准差重算总体、全部阶段、全部 episode、每个关节的原始/归一化误差、误差增量及预测差；与 JSON 在浮点容差内一致。
- 归档的只读独立复核脚本已在当前 Windows 工作区和远程服务器分别执行，三模型均通过。
- 三组 CSV 的 episode、frame、phase、master 原值/零值、目标动作逐行完全一致，JSON protocol 完全一致。
- 三模型阶段标注 SHA256 相同：`463f59def26eec51738613f82e9c670fb8cbd3decc70b81089eabc344918edae`。
- 三份 evaluator 快照 SHA256 相同：`9ff703c86ba21f0d2c59dfdf176063fdfb1205e6c53246ad8b90c2ed5f886c12`；三份 tracked_changes.patch 均为空。
- ACT 记录提交为 `2b14ca50`；DET、INJECT 为 `d9dff543`。后者仅追加文档记录，实际 evaluator 字节相同。
- 三个模型 `.done` 均为 `exit=0`，`complete.done` 为 `all_models_complete`；评估 screen 已退出，没有遗留评估进程。
- 三模型日志未发现 Traceback、RuntimeError、OOM 或非有限值错误。torchvision 视频 API 弃用提示不是本次失败。
- 合成断言与冒烟已在此前通过；未安装 pytest，也不宣称运行过正式 pytest。本次本地仅下载产物并复核，没有运行本地模型评估。

## 7. 建议的后续顺序

1. **优先修正训练—部署输入契约，建立干净的 ACT 基线。** 主臂当前动作不能作为自主策略可见状态。推荐新训练的数据/特征定义中移除 `master_gripper.pos`，保证训练、验证、部署均只使用可观测状态；这是 9→8 维的配置与新 checkpoint 变更，不能只修改旧模型运行时维度。若先做最小成本对照，也可保持 9 维，在训练与验证预处理前统一置零并适配模型，但必须明确记录这是另一种训练协议。
2. **复用同一验证集和第 0 步评估验收新基线，再扩展 DET/INJECT。** 重点检查置零条件下夹爪及抓取/放下阶段是否恢复，并保留 arm、逐关节及逐 episode 指标。不要仅追求含泄漏原输入下的最低离线误差，也不要先根据旧结论调整 INJECT 增益或增加门控。
3. **对启动抖动开展独立的真机短时诊断。** 在供电、标定、初始姿态、相机确认及急停条件满足后，由操作人员先进行不发送动作的目标记录，再进行受限短时执行；记录模型/checkpoint、实际输入状态、预测目标、钳位后发送目标、实测状态和时间戳。分清策略目标、控制环和硬件因素。本次远程评估没有启动机械臂。
4. **最后进行闭环杯位与视觉扰动测试。** 当前 checkpoint 如需选短时诊断候选，INJECT 的部署一致夹爪误差更低，但必须同时检查 arm 与安全约束。成功率、扰动鲁棒性和是否依赖视觉，仍需闭环或独立视觉干预证明；本次只能证明对 master 输入的敏感性，不能单独证明视觉注入的因果收益。

本次没有启动新训练、修改机器人控制逻辑或自动开展真机测试。三个名称中的 s1000 是本次已有训练配置；这里不是多个独立训练 seed 的置信区间实验。

## 8. 文件位置与复核命令

远程项目：`/root/autodl-tmp/lerobot/lerobot-main`。

完整产物目录：`outputs/formal1_C50_phase1/master_gripper_ablation_100k/`。其中包含三组 JSON/CSV/log、命令与代码快照、完成标记、`runner.log` 及 `phase_review/core_phase_annotations.json`。JSON/CSV 和日志已同步到当前 Windows 工作目录的同一路径。

独立复核脚本归档为该产物目录下的 `verify_completed_results.py`，仅读取结果和 checkpoint 归一化统计，不运行模型。服务器可从项目根目录重跑：

```bash
cd /root/autodl-tmp/lerobot/lerobot-main
PYTHONNOUSERSITE=1 /root/miniconda3/bin/python \
  outputs/formal1_C50_phase1/master_gripper_ablation_100k/verify_completed_results.py \
  C50_ACT_s1000 C50_DET_s1000 C50_INJECT_s1000
```

评估启动说明见 [部署一致离线评估运行指南](部署一致离线评估运行指南.md)。已有正式输出应保留，不在原目录覆盖重跑。本次监控报告与工作日志写入本地及远程工作区，未另行提交或推送 GitHub；评估代码此前已同步。
