# C50 无主臂夹爪信号：三模型训练准备记录

2026-09-16。远程项目 `/root/autodl-tmp/lerobot/lerobot-main`，分支 det。

## 当前状态

**数据、三模型训练链路、保存/加载和并发显存检查通过；正式 100k 训练尚未启动，等待保存频率选择。**

用户要求 ACT、DET、INJECT 各 100,000 步，通过三个 screen 并行运行，全部准备完成后再启动。当前只有准备阶段的短时训练完成，没有把冒烟 checkpoint 当作正式训练结果。

## 数据修改与验收

- 原始 `数据集/formal1_C` 保留，没有覆盖。
- 新数据 `数据集/formal1_C50_nomaster` 只包含已验收的 episode 000–049，共 35,900 帧。
- 删除原始状态末维 `master_gripper.pos`，state 从 9 维变为 8 维：六个 follower 关节位置、gripper.load、gripper.curr。
- 六维 action 完全保留，包括夹爪动作；所有其他 parquet 字段逐值核验不变，episode/frame/index 完整且唯一。
- 原始 state 剩余八维与新数据完全一致，状态和动作均为有限值。
- 前 50 集 top 检测标签通过实际 LabelLoader 核验：每个 episode 的 718 帧均有一个 cup 标签，框坐标有限且在 640×480 内；XML 复制后 SHA256 与原始相同。
- 两路 file-000.mp4 以硬链接只读复用，无转码；前 50 集的视频时间偏移保留。原始数据的 info/stats/data SHA256 在转换前后相同。
- 沿用原 40/10 episode 划分：训练 28,720 帧；验证 7,180 帧，episode 为 7、11、13、14、16、27、35、38、40、43。
- 数值归一化统计重新计算且只使用训练 40 集；两路图像沿用 ImageNet mean/std。验证集及后 40 集不参与数值统计。
- 每 episode 的 state 统计也同步从 9 维切为 8 维；info、parquet、统计均一致。
- 数据清单与原始哈希：新数据 `meta/nomaster_manifest.json`。meta 的区间 split 仍表示底层 0:50，真正训练只使用清单中的 40 集，训练脚本显式传入清单。

## 训练设置与准备检查

拟用正式实验名：`C50_NOMASTER_ACT_s1000`、`C50_NOMASTER_DET_s1000`、`C50_NOMASTER_INJECT_s1000`。

三个模型均从头初始化策略，不 resume 旧 checkpoint；保留 ImageNet 视觉预训练。沿用 seed=1000、batch=8、num_workers=4、chunk_size=100、n_action_steps=1、use_amp=false、夹爪损失权重=3、主干 lr=1e-4、策略 lr=1e-5，关闭图像增强、Mask 与 WandB。DET/INJECT 均只监督 top 检测；两者仅 FCOS p4 注入开关不同。

19:06:15 启动三个准备 screen，各训练 20 步；三组均正常退出，并保存 `checkpoints/000020`。冒烟结果独立命名为 `C50_NOMASTER_<模型>_smoke_s1000`，不会续用其权重启动正式训练。

19:08:53 三模型并发资源检查通过：加载冒烟 checkpoint 和保存的处理器，核对训练配置/八维输入/六维动作/训练集统计，解码 episode 0、7、49 的边界样本，验证 batch=8 的 100×6 动作推理有限值，然后通过 barrier 让三个进程同步执行各 10 次真实反向传播及 AdamW 更新。损失和梯度均有限，梯度非零，检测组有 det 损失。

| 项目 | ACT | DET | INJECT |
|---|---:|---:|---:|
| 单进程峰值 allocated MiB | 3765.6 | 4883.2 | 6401.4 |
| 单进程峰值 reserved MiB | 3952 | 5438 | 7104 |

三进程共存的 nvidia-smi 采样峰值 18,458 MiB，剩余 14,149 MiB；无 OOM。采样及检查结果在 `outputs/formal1_C50_nomaster_phase1/preparation/preparation_ready.json`。此文件表示技术检查通过，**不能代替存储预算审批**。

## 正式启动前的存储约束

三组冒烟完整 checkpoint（含可恢复 optimizer 状态）实际总大小为 2,035,314,976 bytes。

| 保存间隔 | 每模型 checkpoint 数 | 三模型预计空间 |
|---|---:|---:|
| 原来的 2,000 步 | 50 | 约 94.8 GiB |
| 建议的 5,000 步 | 20 | 约 37.9 GiB |

服务器当前剩余约 54.6 GiB。原保存频率无法安全完成三组任务。未删除原数据、旧模型或任何既有 checkpoint；未改动 batch、精度或学习率以绕过约束。

已向用户询问是否仅将保存间隔改为 5,000 步后启动。它不改变 100,000 更新步数、模型优化参数或并行要求。**选择未确认前，不执行当前仍为 save_freq=2000 的正式训练命令。**

## 文件与同步

- 转换脚本：`scripts/prepare_c50_nomaster.py`。
- 训练脚本：`scripts/train_c50_nomaster.sh`，每次单独运行一个模型；train 模式有准备检查哈希门槛，不覆盖已存在的实验。
- 保存/加载及并发检查：`scripts/check_c50_nomaster_preparation.py`。
- 技术检查、日志与脚本快照：`outputs/formal1_C50_nomaster_phase1/preparation/`。
- 冒烟日志：`outputs/formal1_C50_nomaster_phase1/C50_NOMASTER_<模型>_smoke_s1000/train.log`；各 `train.done` 为 exit=0。

脚本由本地通过 SCP 同步到远程，新数据和所有训练仅在远程执行；本地没有启动训练。新增脚本尚未另行提交或推送 GitHub，准备目录中已保存实际脚本快照。工作日志与本记录同步到两端。原有 stash、冲突备份和无关用户文件保留。
